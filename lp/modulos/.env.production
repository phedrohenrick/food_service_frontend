# Build de produção (CRA). Não roda o ESLint no build, então warnings não
# derrubam o deploy (na Vercel o CI=true trataria warning como erro).
# TODO: quando os warnings forem limpos, remover esta flag para voltar a lintar no build.
DISABLE_ESLINT_PLUGIN=true
